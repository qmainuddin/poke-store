import { createStackNavigator } from '@react-navigation/stack'

const Stack = createStackNavigator();

const CartStack = () => (
    null
    // <Stack.Navigator>
    //     <Stack.Screen name='CartDetails' component={}></Stack.Screen>
    // </Stack.Navigator>
)

export default CartStack;